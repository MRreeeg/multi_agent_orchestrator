# dsh-agent-pack —— 模型预设示例（不含 API Key）
# 复制为 $DSH_HOME/settings.yaml 后生效；API Key 走环境变量 DEEPSEEK_API_KEY
# 或 $DSH_HOME/.credentials.yaml。
agent-default-model:
  provider: deepseek-official
  model: deepseek-v4-pro
  reasoningEffort: high
